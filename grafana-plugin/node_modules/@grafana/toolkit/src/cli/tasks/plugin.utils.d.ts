import { Task } from './task';
export declare const getToolkitVersion: () => any;
export interface GithubPublishOptions {
    dryrun?: boolean;
    verbose?: boolean;
    commitHash?: string;
    dev?: boolean;
}
export declare const githubPublishTask: Task<GithubPublishOptions>;
